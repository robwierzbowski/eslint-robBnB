// TypeScript rules require a TS config to run, and a TS config requires at
// least one TypeScript file in the repo. I may replace this in the future with
// some .d.ts files to describe exports, but for now I just want a repo with no
// errors.
